# Copy this file to `.env.local` (gitignored) and fill in values as needed.
# After editing, restart the Vite dev server.

# 백엔드 API 베이스 URL.
# 프로덕션 배포 시 실제 서버 주소로 교체하세요.
VITE_API_BASE=http://localhost:3000
